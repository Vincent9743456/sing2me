# mojosong — seule la landing marketing (/site/) est indexable.
# L'application et ses pages publiques (artiste, session Live, liens de
# partage) ne doivent pas être indexées (chantier 3 — paquet défensif).
User-agent: *
Allow: /site/
Disallow: /
